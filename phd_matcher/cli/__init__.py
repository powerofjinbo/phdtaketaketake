"""Console-script entry points (Sprint-3-c5).

Each module exports a `main() -> int` that's wired into `pyproject.toml`
[project.scripts]. The thin shims at `scripts/<name>.py` exist for
running the CLIs directly from a checkout without installing.
"""
